<div class="card game-banner js-search-item" data-search="<%= [game.name, game.organizer, game.site_url, game.ctftime_url].compact.join(' ').downcase %>">
  <div class="game-banner__body">
    <!-- Left: аватар + основная информация -->
    <div class="game-banner__left">
      <%= avatar_image_tag(game.name, url: game.avatar_url, size: 68) %>
      <div class="game-banner__meta">
        <div class="game-banner__title-row">
          <strong class="game-banner__title"><%= link_to game.name, game %></strong>
          <% st = game.status %>
          <% if st == :ongoing %>
            <span class="badge badge--green game-banner__badge">идёт</span>
          <% elsif st == :upcoming %>
            <span class="badge badge--blue game-banner__badge">скоро</span>
          <% elsif st == :past %>
            <span class="badge badge--gray game-banner__badge">прошла</span>
          <% end %>
        </div>

        <div class="game-banner__sub muted">
          <% if game.organizer.present? %>
            <span><%= game.organizer %></span>
          <% end %>
          <% if game.ctftime_url.present? %>
            <span>• <%= link_to 'CTFtime', game.ctftime_url, target: '_blank', rel: 'noopener' %></span>
          <% end %>
          <% if game.site_url.present? %>
            <span>• <%= link_to 'Сайт', game.site_url, target: '_blank', rel: 'noopener' %></span>
          <% end %>
        </div>

        <% if game.services.any? %>
          <div class="game-banner__services">
            <strong>Сервисы (<%= game.services.count %>):</strong>
            <% svc_list = game.services.order(:name).map { |s| link_to s.name, s } %>
            <span class="game-banner__services-list"><%= svc_list.join(' • ').html_safe %></span>
            <span class="muted" style="margin-left:8px; font-size:13px;">
              <%= link_to 'Управлять', manage_services_game_path(game) %>
            </span>
          </div>
        <% end %>
      </div>
    </div>

    <!-- Right: период + окно табло -->
    <div class="game-banner__right">
      <div class="game-banner__times">
        <div class="game-banner__time-title">Период</div>
        <div class="game-banner__time-value">
          <span class="game-banner__icon">🕒</span>
          <div>
            <div><%= game.starts_at %></div>
            <div>— <%= game.ends_at %></div>
          </div>
        </div>
      </div>
      <div class="game-banner__times muted">
        <div class="game-banner__time-title">Табло</div>
        <div class="game-banner__time-value">
          <span class="game-banner__icon">📊</span>
          <div>
            <div><%= game.scoreboard_opens_at || '—' %></div>
            <div>— <%= game.scoreboard_closes_at || '—' %></div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
