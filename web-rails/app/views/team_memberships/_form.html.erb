<%= form_with(model: team_membership) do |form| %>
  <% if team_membership.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(team_membership.errors.count, "error") %> prohibited this team_membership from being saved:</h2>

      <ul>
        <% team_membership.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :team_id, style: "display: block" %>
    <%= form.collection_select :team_id, Team.all, :id, :name, include_blank: false %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.collection_select :user_id, User.all, :id, :display_name, include_blank: false %>
  </div>

  <div>
    <%= form.label :role, style: "display: block" %>
    <%= form.select :role, options_for_select(TeamMembership::ROLES, form.object.role) %>
  </div>

  <div>
    <%= form.label :status, style: "display: block" %>
    <%= form.select :status, options_for_select(TeamMembership::STATUSES, form.object.status) %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
