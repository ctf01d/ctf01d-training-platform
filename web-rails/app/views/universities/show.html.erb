<%= render @university %>

<div>
  <% if user_signed_in? && current_user.role == 'admin' %>
    <%= link_to "Редактировать", edit_university_path(@university) %> |
  <% end %>
  <%= link_to "Назад к университетам", universities_path %>
  <% if user_signed_in? && current_user.role == 'admin' %>
    | <%= button_to "Destroy this university", @university, method: :delete, form: { style: 'display:inline' } %>
  <% end %>
</div>

<hr>
<h2>Команды</h2>
<% teams = @university.teams.order(:name) %>
<% if teams.any? %>
  <ul>
    <% teams.each do |t| %>
      <li><%= link_to t.name, t %></li>
    <% end %>
  </ul>
<% else %>
  <p>Пока нет команд.</p>
<% end %>
