// Default "Общее ТЗ на сервис" template prefilled when a new planning game is
// created. Reproduces the static requirement blocks of the CyberSibir doc as
// editable markdown.
export const DEFAULT_GAME_THEME = "Киберпанк | Дизельпанк | Покемоны | Any";

export const DEFAULT_GAME_REQUIREMENTS = `## Общее ТЗ на сервис

1. Сервис должен быть сетевой (http, https, ws, wss, на tcp/ip udp или ещё какой сетевой протокол хоть ICMP и прочие).
2. Нужен чекер — питоняший скрипт который проверяет работоспособность сервиса: на вход ему подаётся id и флаг.
3. Сервис должен содержать несколько уязвимостей — минимум две, причём хотя бы одна должна быть максимально простой (дефолтный пароль, скрытый функционал и т.д.) для слабых команд.
4. Сервис лучше всего колбасить под докер и под линукс.
5. По вопросам можно писать в личку в телеге @sea5kg.

## Требования к сервису

_под 50 команд, 10 команд профи_

- Максимум 6 сервисов — если хотите делайте, но мы потом выберем лучшие!
- Обязательно логирование в сервисе (для сиемов и прочих сзи).
- Нельзя пересекаться по технологиям! Смотрите остальные заявки на сервис, чтобы выбрать что-то другое.

## Требования к уязвимостям

### НЕ ДЕЛАТЬ уязвимости

- Состояние гонки (участники начинают неправильно пользоваться и создавать бесполезный трафик в сети).
- Любые другие, которые могут привести к забиванию сети бесполезным трафиком.
- Позволяющие провести деструктивную атаку на весь уязвимый образ (по типу «выход из контейнера»).
- Не делать «хардкор» разрыв tcp — забивается незакрытыми сокетами сервер игроков.

### Примеры допустимых

- Дефолтный логин/пароль для административной записи.
- SQL-инъекция.
- Скрытая функция: очистить базу данных.
- Скрытая функция: отправить часть случайных данных на какой-то ip/port.
- RCE через шелл.
- Известные CVE.

## Требования к репозиторию

- **\\d{4}-cybersibir-service-имясервисавашего** — приватный реп в группе github.com/sibirctf.
- **vuln-service** (обязательна) — директория с сервисом, то что будет поставляться, должен быть docker-compose up.
- **checker_<idservice>** (обязательна) — директория с чекером (зависимости описать в checker_<idservice>/README.md).
- **writeups** (обязательна) — директория с описанием уязвимостей и их эксплуатацией.
- **exploits** (обязательна) — директория со скриптами, показывающими как эксплуатировать уязвимости.
- **README.md** (обязателен) — файл с описанием сервиса: для какой игры, кто автор и общий дескрипшн.
`;
